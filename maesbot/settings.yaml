# These paths are INSIDE the Docker container.
# Docker Compose maps your host directories to these container paths.
#
# Host Path (Zorin/Asahi)              → Container Path
#
# /home/hanne/.../docs/_data/          → /data/input/
# /home/hanne/.../maesbot-private-data → /data/private/

paths:
  private_settings: "/data/private/private-settings.yaml"
  punten_input_dir_website: "/data/input/"
  punten_output_dir: "/data/private/Punten/Generated Output/"
