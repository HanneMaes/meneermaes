title: "Database Design"
assignement:
  - desc: "**Create an ERD** with at least 5 entities and clear relationships."
    max: 10
  - desc: "Implement the database in Access. *Include primary & foreign keys.*"
    max: 12
  - desc: "Use a **form with validation** to input new data."
    max: 8
